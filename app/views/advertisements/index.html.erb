<h1>All Advertisements</h1>

<% @advertisement.each do |ads| %>
    <div class="media">
        <div class="media-body">
            <h4 class="media-heading">
               <%= link_to "#{ads.title} for $#{ads.price}" %>
            </h4>
        </div>
    </div>
<% end %>